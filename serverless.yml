service: file-upload

frameworkVersion: '3'

provider:
  name: aws
  runtime: java11

package:
  artifact: target/hello-dev.jar

functions:
  hello:
    handler: com.serverless.Handler
    events:
      - http:
          path: /HolaMundo
          method: get

plugins:
  - serverless-offline